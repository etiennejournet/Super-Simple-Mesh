---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: test
  name: test
  namespace: default
spec:
  ports:
  - name: 80-80
    port: 8000
    protocol: TCP
    targetPort: 80
  selector:
    purpose: test
  type: ClusterIP
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: test2
  name: test2
  namespace: default
spec:
  ports:
  - name: 80-80
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    purpose: test2
  type: ClusterIP
---
apiVersion: v1
kind: Pod
metadata:
  name: test2
  labels:
    purpose: test2
  annotations:
    cert-manager.ssm.io/service-name: "test2.default.svc.cluster.local"
spec:
  containers:
  - name: test
    image: nginx
  restartPolicy: OnFailure
---
apiVersion: v1
kind: Pod
metadata:
  name: test
  labels:
    purpose: test
  annotations:
    cert-manager.ssm.io/service-name: "test.default.svc.cluster.local"
spec:
  containers:
  - name: test
    image: nginx
  restartPolicy: OnFailure
