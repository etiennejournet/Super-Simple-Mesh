---
apiVersion: v1
kind: Pod
metadata:
  name: test2
  labels:
    purpose: test2
  annotations:
    cert-manager.ssm.io/enabled: "true"
spec:
  containers:
  - name: test
    image: nginx
  restartPolicy: OnFailure
